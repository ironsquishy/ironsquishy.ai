inherits: "configs/base/training.yaml"

base_model: "TinyLlama/TinyLlama-1.1B-Chat-v1.0"
output_dir: "adapters/tinyllama-latest"

device_overrides:
  cuda:
    max_length: 512
    per_device_train_batch_size: 1
    gradient_accumulation_steps: 8
    use_4bit: true
    fp16: true