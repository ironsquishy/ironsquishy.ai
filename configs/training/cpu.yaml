inherits: "configs/base/training.yaml"

num_train_epochs: 1
lora_r: 4
lora_alpha: 8

device_overrides:
  cpu:
    max_length: 128
    per_device_train_batch_size: 1
    gradient_accumulation_steps: 4
    use_4bit: false
    fp16: false